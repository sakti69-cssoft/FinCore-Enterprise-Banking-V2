apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-fincore
spec:
  selector: {app: fincore}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
  type: {{ .Values.service.type }}
