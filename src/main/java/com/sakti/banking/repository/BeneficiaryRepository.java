package com.sakti.banking.repository; import com.sakti.banking.entity.Beneficiary; import org.springframework.data.jpa.repository.JpaRepository; import java.util.*; public interface BeneficiaryRepository extends JpaRepository<Beneficiary,Long>{ List<Beneficiary> findByOwnerEmailIgnoreCaseOrderByCreatedAtDesc(String email); boolean existsByOwnerIdAndAccountNumber(Long id,String n); }
